import AuthStyeldComponents from 'screens/Auth/styledComponents-shared';
import styled from 'styled-components/native';

const SetPasswordText = styled(AuthStyeldComponents.MEDIUM_16PT_TEXT)`
  margin-top: 24px;
  margin-bottom: 16px;
`;

const SetPasswordComponents = {
  SetPasswordText,
};

export default SetPasswordComponents;
