import styled from 'styled-components/native';

const MEDIUM_16PT_TEXT = styled.Text`
  font-size: 16px;
  color: ${props => props.theme.color.fontblack};
  letter-spacing: -0.8px;
  font-weight: 500;
`;

const AuthStyeldComponents = {
  MEDIUM_16PT_TEXT,
};

export default AuthStyeldComponents;
