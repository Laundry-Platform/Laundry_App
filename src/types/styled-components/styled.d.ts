import 'styled-components/native';

declare module 'styled-components/native' {
  export interface DefaultTheme {
    color: {
      blueberry: string;
      marigold: string;
      main: string;
      softblue: string;
      fontlight: string;
      graylight: string;
      fontblack: string;
      gray: string;
      red: string;
    };
  }
}
